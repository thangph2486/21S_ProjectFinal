/**
 * 
 */

class ShopItem {
    constructor(id, name, price, inStoke, discount) {
        this.id = id,
            this.name = name,
            this.price = price,
            this.inStoke = inStoke,
            this.discount = discount
    }
}